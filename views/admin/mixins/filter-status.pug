mixin filter-status(filterStatus)
  .d-flex.flex-wrap
    each item in filterStatus
      button(
        class=`btn btn-sm me-2 mb-2 btn-outline-success ${item.class}`
        button-status=item.status
      ) #{item.name}
